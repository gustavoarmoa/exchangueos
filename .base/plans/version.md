# Allenty ExchangeOS — Version

**Current:** `4.23.0`

## Policy

Allenty ExchangeOS follows [SemVer](https://semver.org/):

- **MAJOR** — Structural reorganization (new workstreams, hierarchy changes, monolithic → modular split)
- **MINOR** — New milestones, epics, or significant document additions
- **PATCH** — Content updates, fixes, typo corrections

## History

| Version | Date | Description |
|---------|------|-------------|
| **4.23.0** | **2026-05-26** | **MINOR — Local CRUD admin API + 7 new seeds + 30 tables ≥ 5 rows + full POST/PUT/DELETE/LIST/GET lifecycle proved end-to-end** — Direct response to "criar CRUDs de cada tipo de registros no database local no mínimo 5 cada, para poder realizar queries". Plan delivers across 5 phases per the pre-presented plan, all OK'd by user. **Phase 1 — Sample data (7 new seed files + tenants extended):** `seeds/07_actors_audit.sql` (5 actors + 5 audit_events), `seeds/08_trades_amendments.sql` (5 counterparties + 5 fx_trades covering Spot EURUSD / NDF USDBRL / Forward GBPUSD / Swap USDJPY near+far + 5 trade_amendments showing 4-eyes lifecycle), `seeds/09_quotes.sql` (5 rfqs + 5 quotes + 5 quote_streams), `seeds/10_settlement.sql` (5 cls_cycles + 6 cls_cycle_trades + 5 payin_instructions across PIN1/PIN2/PIN3 + 5 net_reports), `seeds/11_risk_position.sql` (5 risk_limits across all 5 limit_types + 5 positions), `seeds/12_compliance_admin.sql` (5 classifications + 5 iof_computations + 5 bacen_reports + 5 screening_results + 5 system_events + 5 eod_jobs), `seeds/13_outbox.sql` (3 pending outbox_events that worker auto-drains + 2 in-table dispatched + 5 archive rows). `seeds/00_tenants_dev.sql` extended from 1 → 5 tenants (DEV + 3 sample banks + sandbox). All seeds FK-respecting + idempotent (`ON CONFLICT DO NOTHING`). **Phase 2 + 3 — Admin API (`internal/adminapi/`):** new package with `registry.go` (30-table catalogue with PK + tenant scoping + allowed filters + default order + mutability per table) + `handler.go` (gin Handler with LIST + GET + POST + PUT + DELETE methods, value normalisation `[16]byte→uuid` + `driver.Valuer→string` for precision-preserving decimals, tenant scoping via `X-Tenant-Id` header defaulting to dev tenant, query-param filters allowlisted to avoid SQL injection, pagination via `limit` 100/500 + `offset`, introspection via `information_schema.columns` for INSERT/UPDATE field allowlisting). Routes wired in `cmd/api/main.go` gated by `EXCHANGEOS_ENABLE_ADMIN_API=true` (production-safe — must be explicit). 6 routes registered: `/v1/admin/_schemas` + `/v1/admin/:table` + `/v1/admin/:table/:id` × GET/POST/PUT/DELETE. `audit-events` + `outbox-archive` are read-only (Mutable=false → 405 on POST/PUT/DELETE per ISO 27001 + regulatory). **Phase 4 — Smoke + integration tests:** `scripts/smoke-crud.sh` enumerates every exposed table via `/_schemas`, LISTs each with `limit=500`, asserts count ≥ `MIN_ROWS` (default 5) — **30/30 passing**. `tests/integration/admin_crud_test.go` covers 6 scenarios: full Currency POST→GET→PUT→GET→DELETE lifecycle with deferred cleanup, audit-events POST returns 405, outbox-archive DELETE returns 405, unknown table returns 404, filter `status=SETTLED` returns exactly 1, pagination `limit=3&offset=3` non-overlapping — **all 6 green**. **Phase 5 — Docs:** `docs/operations/local-crud-guide.md` (endpoint catalogue + curl examples + direct CRDB SQL queries for analytics use cases + production safety policy: must oauth2-proxy + separate exchangeos_admin DB role). **Compose docker-compose.yml** updated: api gets `EXCHANGEOS_ENABLE_ADMIN_API=true`; migrator/seed/grant-app/kafka-init chain unchanged. **Two schema fixes** in registry to handle composite-PK tables (currency_pairs, netting_cutoffs, cls_cycle_trades, calendar_holidays) — PK column set to first segment + filter exposes both halves; LIST + filter works perfectly; GET-by-id is best-effort for composite (returns first match). Stack state: 4 long-running containers (api healthy, worker dispatching, crdb+kafka healthy, otel collecting) + 4 one-shot services (crdb-init, migrator no-change, seed 14 files applied, grant-app, kafka-init 14 topics). **Test scoreboard: 6 integration + 30 smoke = 36 new assertions all green.** Build clean (`go build ./...` exit 0). |
| 4.22.0 | 2026-05-24 | **MINOR — MS-024 cycle advance: go.sum unblocked + 14 erasure tests green + NetReport repo + classifier wired to AllNatureCodes** — Four-axis sprint advance with measurable code+tests, not just docs. **(a) Repo-wide go.sum gap resolved:** `buf generate` materialised `proto/gen/exchangeos/v1/` (19 files across 9 services + grpc + common); `go mod tidy` then resolved all transitively-missing entries (shopspring/decimal, testify, pgx, uuid, gin, 30+ others). Test runners now functional. Pre-existing `internal/telemetry` compile error (`semconv.DeploymentEnvironment` undefined — API drift in upstream otel/semconv) is the **only remaining repo-wide build issue** — not introduced here. **(b) MS-024e completion advance — classifier wired to generated catalogue:** Resolved naming collision by renaming generated type `NatureCode` → `NatureCodeFull` (keeping legacy slim type unchanged for backward compat); regenerated `pkg/bacen/codes_full.go` (543 lines, 46 codes, `AllNatureCodes map[string]NatureCodeFull`); added `FullByCode` accessor + `CountByCategory` helper. `Classifier.ByCode` now consults `AllNatureCodes` first (compressing to slim `NatureCode` via `deriveNature(direction)` mapping INGRESSO→Ingresso, REMESSA→Remessa, BIDIRECTIONAL→Conversao); falls back to legacy `builtin` only on miss. `data/bacen/golden-classifications.csv` seeded with 18 curated phrase→code pairs covering all 10 categories. `pkg/bacen/codes_full_test.go` 6 new tests (catalogue populated, all 10 categories covered, 5 known codes resolve, classifier resolves generated codes via new path, legacy fallback works for 60001 not-in-generated, `deriveNature` cases including unknown→Remessa default). **(c) MS-024h advance — NetReport repo (2 of 6 BCs):** `modules/netreport/infrastructure/postgres/repos.go` `NetReportRepo` against migration 000006 with UPSERT on `(cycle_id, currency)` UNIQUE (NetReports idempotent — regen overwrites latest gross/net), `GetByCycleCcy(cycleID, currency)`, `ListByCycle` ordered alphabetically by currency; `modules/netreport/domain/reconstitute.go` `ReconstituteNetReport` helper preserving precomputed `netSettlement` (caller responsible for triplet consistency); compile-time `var _ application.Repository = (*NetReportRepo)(nil)` check; shared scanner pattern. **(d) MS-024a Stage 2 — executor scaffold:** `internal/erasure/executor.go` driver-agnostic `DB`/`Tx` interfaces (pgx/v5 + database/sql both satisfy via thin adapters, package stays vendor-free), `AuditEmitter` interface for `OpAudit` + `CompletionAudit` events (production wraps modules/admin pipeline + outbox), `Executor.Apply` running one transaction per Operation with order preservation + per-op audit emission + final completion event + explicit non-rollback policy on partial audit failure (ISO 27001 8.15 logging — partial evidence is still evidence), `BuildSQL` exported for `--dry-run` consumption with deterministic field-sort + single-quote escape; safety note documents WHY the WHERE clause is interpolated verbatim (plan is co-signed Stage 3 artefact, not user input) and how validate refuses empty WHERE; `HashSamples` SHA256 helper kept stub for Stage 3 snapshot-diff feature. `internal/erasure/executor_test.go` 6 new tests (BuildSQL redact with deterministic sort, BuildSQL hard_delete, happy-path Apply with mock DB/audit + rows-total aggregation, refuses without approvals, rolls-back on exec error + zero audit emitted, HashSamples stable + 64-char). `cmd/erasure-worker/main.go` execute path updated with documented MS-024a Stage 3 wiring recipe (still rejects --execute until concrete DB+audit adapters land). **Final test count this turn:** 14 erasure (`internal/erasure`) + 6 new bacen (`pkg/bacen` total now ~33) + existing netreport+payin domain/application all passing under the new go.sum. Build clean across all touched packages (`go build ./internal/erasure/... ./cmd/erasure-worker/... ./pkg/bacen/... ./modules/payin/... ./modules/netreport/...` exit 0). Dashboard unchanged (26 delivered / 3 active / 10 backlog); milestones MS-024a/e/h all noted as having Stage-N gates remaining before they can move DELIVERED. |
| 4.21.0 | 2026-05-24 | **MINOR — MS-024 cycle kickoff: 3 milestones moved BACKLOG → ACTIVE + initial deliveries** — Plan total still 39 milestones (26 MS-023 delivered + 13 MS-024); now 3 of MS-024 in ACTIVE (MS-024a + MS-024e + MS-024h), reducing backlog from 13 → 10. **(a) MS-024h** — first of 6 postgres repos delivered: `modules/payin/infrastructure/postgres/repos.go` `PayInRepo` against migration 000006 with UPSERT + optimistic version guard (`WHERE version = EXCLUDED.version - 1`) + `application.ErrInvalidInput` returned on RowsAffected=0; `modules/payin/domain/reconstitute.go` `ReconstitutePayIn` persistence-boundary helper bypassing constructor validation; compile-time `var _ application.Repository = (*PayInRepo)(nil)` check; nullable time/string helpers + `pgtype.Numeric → decimal.Decimal` round-trip via string preserving precision; ListByCycle ordered `(currency, deadline)` for stable output. This is the **reference impl** for the remaining 5 BCs (netreport / compliance / admin / cfets_capture / cfets_confirmation) — pattern documented in package doc. **(b) MS-024e** — BACEN nature codes pipeline operational: `data/bacen/nature-codes-circ-3690-v20260101.csv` source-of-truth with **46 codes** across 10 categories (COMERCIAL + SERVICOS + CAPITAL + TRANSFERENCIAS + TURISMO + CARTAO + RENDA + DERIVATIVOS + OUTROS + VASP) including direction (INGRESSO/REMESSA/BIDIRECTIONAL) + minimum doc requirements (e.g. `INVOICE+DUE`, `SCE-IED+CONTRACT`) + IOF op type mapping; `pkg/bacen/codegen/main.go` deterministic generator (CSV → Go map, alphabetical-by-code, text/template, ~150 LOC zero-deps); `pkg/bacen/codes_full.go` generated (543 lines, 46 codes) with `AllNatureCodes` map + `CountByCategory()` helper; `data/bacen/README.md` documents schema + versioning policy + CSV→Go pipeline; `//go:generate` directive added to `classifier.go`. Smoke-tested in isolated workspace — 46 codes written deterministically. **(c) MS-024a** — erasure-worker scaffold operational: `internal/erasure/plan.go` `Plan` + `Operation` types + `ParsePlan` (JSON, no external deps — DPO UI is expected to render YAML for human review and convert to JSON before signing) + `Plan.Validate` (ticket must start with `LGPD-`, where must be non-empty to refuse full-table mutation, redact requires fields, hard_delete forbids fields, unknown op rejected) + `HasRequiredApprovals` (case-insensitive check for both `dpo` AND `compliance_officer`) + `RedactionMarker` returning canonical `[REDACTED PER LGPD ART 18 IV <ticket>]`; `internal/erasure/plan_test.go` 8 test cases (valid round-trip + 5 negative validations + 2 approval checks); `cmd/erasure-worker/main.go` CLI accepting `--ticket` + `--plan` + mutually-exclusive `--dry-run|--execute` flags; dry-run path implemented end-to-end (prints SQL preview per op); execute path gated by `HasRequiredApprovals` + `EXCHANGEOS_ERASURE_CONFIRM=YES-I-MEAN-IT` env guard against accidental CI/shell-history invocation; structured slog JSON output. Executor (CRDB tx + audit emit + outbox event) deferred to MS-024a Stage 2. **Build check:** `go build ./internal/erasure/... ./cmd/erasure-worker/... ./pkg/bacen/codegen/...` clean. Tests blocked by pre-existing repo-wide go.sum gap (shopspring/decimal, testify, pgx, uuid, gin missing) — separate cleanup needed (tracked as new MS-024 sub-task). Dashboard reflects: 26 delivered / 3 active / 10 backlog = unchanged 26/39 overall delivered, but active count flipped 0 → 3 indicating real work in flight. |
| 4.20.0 | 2026-05-24 | **MINOR — MS-024 production-hardening cycle opened (13 milestones in backlog)** — Direct response to honesty review on v4.19.0 distinguishing "planning complete" from "production-ready". Plan total now **39 milestones** (26 MS-023 delivered + 13 MS-024 backlog); MS-023 cycle remains at 100% (26/26) — completion semantics unchanged. **13 new milestone files in `milestones/backlog/`** each mapping 1:1 to a known gap, with full structure (status BACKLOG + phase F-OPS-PROD + sprint hint + dependencies + description + acceptance criteria + deliverables + cross-references): **MS-024a** LGPD Erasure Worker (`cmd/erasure-worker/` + plan JSON schema + 4-eyes enforcement + audit emit + Helm manual-trigger CronJob), **MS-024b** Archival Cron Worker (`cmd/archiver/` + `archive_policy` config table + parquet/GCS Coldline writer + SHA256 manifest + retention-after hard-delete + restore-drill harness), **MS-024c** Credential Rotator real loop (`cmd/cred-rotator/` + KeycloakOS admin API + Vault writes + rolling restart + monthly CronJob + emergency rollback), **MS-024d** Live Sanctions Providers (4 providers OFAC/UN/EU/COAF behind `SanctionsProvider` interface + Jaro-Winkler scorer + hourly refresh cron + 24h staleness alert), **MS-024e** Full BACEN Nature Code Catalogue (CSV source-of-truth + `go generate` codegen + 95 codes + 200-phrase golden corpus + ≥ 95% accuracy gate), **MS-024f** BACEN Submission Adapters (4 submitters DEC + SCE-IED + SCE-Credito + SCE-CBE + mTLS client + XAdES signature + mock gateway for CI + circuit breaker), **MS-024g** SISCOAF COS Submission (COSCase aggregate + 4-eyes review queue + `pkg/siscoaf` marshaller + 20h SLA alert per RN_FX_039), **MS-024h** Complete Postgres Repository Layer (6 BCs payin/netreport/compliance/admin/cfets-capture/cfets-confirmation × 8 CRUD ops + Reconstitute helpers + container wiring + dual-execution test harness), **MS-024i** CRUD Test Suite Completion (14 BCs × 8 ops = 112 + 14 concurrency + 5 saga = ≥ 131 integration tests under `//go:build integration` + per-test schema isolation + coverage ≥ 70%), **MS-024j** E2E Scenario Completion (7 remaining of 10 catalogued: 02 NDF + 03 CFETS capture + 04 CFETS confirmation + 06 position + 07 CLS cycle + 09 sanctions + 10 EOD batch + `.github/workflows/e2e.yml` boots `docker compose up` for each), **MS-024k** Pattern Catalogue Build-out (target 300 of 850 fully written + pattern lint hook + TEMPLATE.md + quarterly review meetings + "pattern-per-PR" tax), **MS-024l** CRDB Hub TLS Cross-repo PR (open + drive to merge the spec from `docs/operations/crdb-hub-tls-pr.md` + Vault cert pop + staging connectivity proof + go-live-checklist update), **MS-024m** Production Deployment (close-out: Terraform applied + cert-manager + ArgoCD + Argo Rollouts + Litmus + Chaos Mesh + External Secrets + ArgoCD sync green + smoke + load + canary + chaos + DR drill + on-call enrolled + first chaos day scheduled). Plus updates: `milestones/index.md` version 1.0.0 → 1.1.0 with 2 new sections (cycle MS-023 + cycle MS-024 with gap-mapping table + MS-024 file inventory + suggested sprint plan 4 sprints), `roadmap/master-plan.md` version 1.0.0 → 2.0.0 introducing **Ciclos** section explicitly separating "planning + scaffolding" from "production hardening" + timeline extended Sprint 20-23 + summary table row added for MS-024. Sprint plan: Sprint 1 (infra parity: 024a+b+c+h+l), Sprint 2 (compliance correctness + tests: 024d+e+i+j), Sprint 3 (regulatory submission: 024f+g), Sprint 4 (production deploy close-out: 024m), Background (pattern build-out: 024k). **Dashboard reflects new total 39 milestones** with MS-023 at 100% (26/26) but the planning-as-a-whole now 26/39 = 67% on the new aggregate metric (production-ready scope).** |
| 4.19.0 | 2026-05-24 | **MINOR — FinOps + cost guardrails + Chaos engineering program + Data lifecycle + LGPD evidence** — Plan remains 100% (26/26). Three post-closure layers addressing financial accountability, resilience validation, and regulatory privacy hygiene. **(a) FinOps + cost guardrails:** new Terraform module `deploy/terraform/modules/exchangeos-budget/` creating GCP Billing budgets with 5 progressive thresholds (50/80/100/120% current spend + 100% forecasted) + Pub/Sub alert routing + 2 sub-budgets (compute 60% / storage 15%) — plus per-resource cost-allocation labels enforced via Terraform `default_labels` + Helm `commonLabels` policy (`docs/operations/cost-allocation.md` — 5 mandatory labels module/env/bc/tier/cost_center, fallback policy for label-incapable services, label-drift treated as P3 bug). Operational artefacts: `docs/operations/cost-review-template.md` (quarterly review template with spend buckets + per-BC allocation + threshold events + right-sizing opportunities + sign-off table), `deploy/grafana/dashboards/exchangeos-finops.json` (8-panel FinOps board: MTD spend stat with thresholds, forecasted month-end via `predict_linear`, daily spend timeseries 30d, spend-by-service pie, spend-by-BC bargauge, **unlabeled spend table** to catch allocation escapees, GitHub Actions minutes + lefthook-pre-empted savings). 4-tier escalation policy: 50% informational → 80% warning → 100% page on-call FinOps → 120% escalate CTO + freeze infra growth. **(b) Chaos engineering program:** `docs/security/chaos/README.md` codifies 5 principles (steady state first, smallest blast radius, stop conditions, hypothesis-driven, always-writeup) + **10-scenario catalogue** (CHAOS-01..10 covering pod kills, node drain, network latency/loss, region failover, Vault outage, Identos discovery 503, PTAX feed unreachable, Kafka broker death) each with hypothesis + steady-state SLI + severity. Tooling: Litmus 3.x ChaosCenter + Chaos Mesh + kubectl drain + Toxiproxy + k6. Cadence: weekly automated (CHAOS-01/02 staging) + monthly manual + quarterly chaos day + annual production-canary. Manifests: `deploy/chaos/chaos-pod-kill.yaml` (Litmus ChaosEngine for api + worker with Prometheus probes — 5xx<1% + p99<200ms + outbox recovery<300s), `deploy/chaos/chaos-network-latency.yaml` (Chaos Mesh NetworkChaos 200ms delay + 20ms jitter api→CRDB), `deploy/chaos/chaos-network-loss.yaml` (10% packet loss worker→Kafka with idempotent-producer dedup verification). `docs/security/chaos/chaos-day-runbook.md` (T-1 week prep checklist + 4h timed schedule for 5 experiments + per-experiment script + stop-the-day criteria + annual escalation to prod-canary). `docs/security/chaos/experiment-template.md` (copy-per-run with hypothesis + abort criteria + timeline + result + action items + artefacts-updated checklist). **(c) Data lifecycle + LGPD evidence:** `docs/security/data-lifecycle/README.md` introduces 4-tier data classification (C1 Public / C2 Internal / C3 Confidential / C4 Restricted-PII) + per-table retention schedule covering **25 tables** with class + legal basis + retention + archival trigger + right-to-erasure scope (10y regulatory hold for fx_trades/bacen_reports/cls_cycles/payin/net_reports/audit_events/classifications/iof_computations/system_events/positions; 7y for tenants/counterparties/screening; 5y for quotes/actors/ssis post-relationship; indefinite for refdata). `docs/security/data-lifecycle/erasure-workflow.md` formalises 5-stage LGPD Art. 18 IV right-to-erasure workflow (intake → identity verification 3d → eligibility check 7d → execution 12d with 4-eyes co-sign → subject response 15d → post-completion audit 30d) + edge cases (audit-only subjects, employee actors, request-volume spike Art. 19 §4 extension). `docs/security/data-lifecycle/archival-cron.md` specifies `cmd/archiver` nightly job (02:00 SP) driven by `archive_policy` config table + per-batch GCS Coldline writes with SHA256 manifest in `archive_manifest` + retention-after hard-delete + outbox event + quarterly restore-from-archive drill. Terraform `google_storage_bucket.exchangeos_archive` shape included (SOUTHAMERICA-EAST1 + COLDLINE → ARCHIVE after 1y + CMEK encryption + 10y retention policy). 3 archival SLOs: daily job 100% / 30d, completeness < 1% behind, restore < 30min. `scripts/lgpd-eligibility.sh` portable bash 3.2+ READ-ONLY discovery script — UUID-validates subject ID, queries 6 hot tables (actors/counterparties/fx_trades/audit_events/quotes/screening_results) returning per-table row count + earliest occurred_at + regulatory-hold status + eligibility decision (ELIGIBLE_HARD_DELETE / ELIGIBLE_REDACT / DEFERRED_UNTIL_<date> / FROZEN_REGULATORY). Output drives the 4-eyes DPO+Compliance execution plan reviewed before any mutation. **Dashboard unchanged at 100% (26/26).** |
| 4.18.0 | 2026-05-24 | **MINOR — Performance baseline + ADRs + onboarding handbook + SLI/SLO formalisation + 3 future-sibling integration contracts** — Plan remains 100% (26/26). Three post-closure layers focused on engineering-rigor + organisational scaling. **(a) Performance baseline + regression gate:** benchmark suites added across the hot paths — `pkg/pricing/cip_bench_test.go` (BenchmarkForward_360 < 5µs target + BenchmarkCross_EURUSD_USDBRL triangulation + BenchmarkPositionMTM), `pkg/outbox/outbox_bench_test.go` (BenchmarkDispatch_HotPath + BenchmarkDispatch_Batch100 + BenchmarkRecord_Build, target < 50µs/record), `modules/trade/domain/fxtrade_bench_test.go` (BenchmarkNewFXTrade < 10µs + BenchmarkLifecycle_BookConfirmSettle full happy-path < 50µs). `.github/workflows/benchmarks.yml` matrix-runs benchstat across the 3 packages, caches baselines per `${{ github.base_ref }}`, warn-only comparison until regression policy ratifies block. `docs/operations/performance-baseline.md` codifies current numbers + 4-tier regression policy (< 10% silent / 10-20% warn / > 20% block PR / > 50% auto-revert + post-mortem) + profiling toolkit pointers (pprof CPU/heap/block + `go test -trace`) + linkage back to SLI/SLO doc. **(b) Architecture Decision Records:** `docs/adr/` initialised with MADR-format README + **8 ADRs** documenting load-bearing architectural choices: ADR-0001 Shared CRDB hub TLS (vs per-module clusters; cost rationale 3 nodes vs 39+), ADR-0002 DDD 14 bounded contexts (uniform domain/application/infrastructure/api layering), ADR-0003 Transactional outbox (aggregate state + outbox row in same DB tx; worker polls + publishes; at-least-once + consumer idempotency), ADR-0004 Build-tag gated bindings (`//go:build grpcgen` + `//go:build kafka` with paired no-op files), ADR-0005 Decimal-only money (shopspring/decimal mandatory + forbidigo `float64` ban outside tests), ADR-0006 Workload Identity Federation (zero JSON keys via roles/iam.workloadIdentityUser binding), ADR-0007 GitOps ArgoCD (pull-based vs imperative kubectl), ADR-0008 Argo Rollouts canary (4-step 10→30→60→100 with AnalysisTemplate gates: 5xx < 1% + p99 < 500ms, failureLimit=3 auto-rollback). **(c) Operational maturity:** `docs/onboarding/README.md` engineering onboarding handbook with day 1 (local setup + first PR + smoke curl) → day 7 (13-folder repo tour table) → day 30 (own a BC + ship a feature end-to-end) → day 90 (full member — on-call + tabletop drill + ADR authorship + lead canary deploy) ramp + key conventions cited from CLAUDE.md. `docs/operations/sli-slo-definitions.md` formalises **8 SLIs/SLOs** with explicit PromQL queries: API availability (99.9% / 30d budget 43.2 min), p99 quote latency (< 100ms / 7d), p99 trade booking (< 200ms / 7d), outbox dispatch lag (< 5 min / 24h), CLS cycle on-time (100% / 30d zero tolerance), BACEN report success (≥ 99% / 30d), worker availability (99.9% / 30d), CRDB p99 query (< 50ms / 7d) + 2-window burn-rate alert pattern (Google SRE Workbook) + error-budget exhaustion policy (50% → halt non-emergency deploys, 100% → SLA credits + RCA) + monthly/quarterly/annual review cadence. **3 more sibling integration contracts** documenting design intents for future siblings: `docs/integrations/riskos.md` (ExchangeOS produces risk.breach.v1 + position.snapshot.v1; consumes group_risk.limit_pressure.v1 advisory-only; no sync RPCs initially), `docs/integrations/complos.md` (ExchangeOS consumes sanctions.list_updated.v1 + policy.bacen_code_added.v1 + kyc.actor_status_changed.v1; produces compliance.cos_required.v1; sync SanctionsScreening + ComplianceQuery RPCs with 5s timeout + 24h cache fallback), `docs/integrations/treasuryos.md` (ExchangeOS produces settlement.payment_required.v1 + cls.payin_required.v1 + position.snapshot.v1; consumes liquidity.unavailable.v1 + nostro.balance_snapshot.v1 + hedge.proposal.v1; sync LiquidityQuery for large notionals with hard-block for forwards > USD 10M + 200ms p99 timeout). `docs/integrations/README.md` integration matrix expanded from 5 to 8 rows + design-intent column. **Dashboard unchanged at 100% (26/26).** |
| 4.17.0 | 2026-05-24 | **MINOR — Production automation + audit packaging + sibling integration contracts** — Plan remains 100% (26/26). This release adds 3 post-closure operational layers. **(a) Production deployment automation:** `scripts/smoke-prod.sh` (production-grade smoke validation — 7 checks across /healthz, /readyz, /version, /v1/refdata/currencies + correct-error-code 404/400 propagation + optional gRPC health.Check; STRICT=true gating for canary AnalysisTemplate consumption; exits non-zero with first failing check name), `tests/load/k6-trade-book.js` (k6 sustained-load spec — warmup 30s × 5 VUs + ramping 50→100→100→10 over 10min, custom metrics latency_healthz/latency_currencies, SLO-aligned thresholds 5xx<1% + p99 healthz<200ms + p99 currencies<500ms + errors<1%, summary handler writes machine-readable JSON to `tests/load/results-last.json`), Taskfile additions (`canary:status/promote/abort/rollback` proxying `kubectl argo rollouts` + `smoke:prod` + `load:trade-book` + `audit:bundle`), `docs/operations/runbook-index.md` (single-page index aggregating 6 runbooks across deployment/security/day-2/observability/comms/architecture + external dependencies table). **(b) ISO 27001 evidence package:** `docs/security/iso27001-gap-tracker.md` (5 🟡 partial + 18 ⏳ deferred controls with owner + action + ETA — 4 in 2026-Q3, 2 in 2026-Q4, target ≥ 90% pre-audit), `docs/security/audit-interview-prep.md` (14 likely-asked questions across 7 themes A-G + canned answers + evidence file pointers per Q + bring-along checklist for auditor visit), `scripts/audit-evidence-bundle.sh` (read-only collection script producing `.audit-bundles/exchangeos-evidence-YYYYMMDD-HHMM.tar.gz` organising 6 categories: 01-governance + 02-security + 03-operations + 04-technical + 05-compliance + 06-delivery; emits SHA256 for separate-channel integrity check + missing-file warnings), `docs/security/drills/template.md` (tabletop drill template with timeline + observations + action items + sign-off; cadence quarterly). **(c) Sibling integration contracts:** `docs/integrations/README.md` (5-module integration matrix + out-of-scope rationale for 6 more siblings + common contract conventions + versioning policy), per-sibling docs `ledgeros.md` (trade.settled.v1 → posts journal; sync reconciliation API; chart-of-accounts open Qs), `accountos.md` (tenant.created/updated/suspended/actor.created/disabled CDC → exchangeos.tenants materialised view; ResolveTenant sync fallback 5s × 3 retries + 5min cache; LGPD right-to-erasure open Q), `paymentos.md` (settlement.payin_requested ↔ payment.settled/failed for PvP coordination; CommitPvP sync RPC 10s timeout authoritative; CLS atomicity open Q), `authorityos.md` (compliance.report_ready + cos_required → BACEN/SISCOAF submission; regulator.response_received + policy_updated consumption; LGPD per-screening retention), `identos.md` (KrakenD JWT validation + claim extraction `x-actor-sub/x-tenant-id/x-scope/x-correlation-id` + 14-secret M2M client catalog managed by cmd/cred-rotator monthly cron). |
| 4.16.0 | 2026-05-24 | **MINOR — Post-closure operational additions: go-live artifacts + standards expansion + ISO 27001 audit prep** — Plan remains 100% complete (26/26 milestones); this release adds OPERATIONAL + AUDIT artifacts beyond the original scope to enable real go-live + cert engagement. **(a) Production go-live (4 artifacts):** `docs/operations/crdb-hub-tls-pr.md` (cross-repo PR spec for `cockroachdb/modules/exchangeos/` with database.sql + users.sql + cert template + DSN template + acceptance criteria + rollback), `scripts/vault-seed.sh` (idempotent operator script seeding `secret/data/exchangeos/{db,oidc,kafka}` + applying read-only policy + binding K8s ServiceAccount via auth/kubernetes — fail-loud on missing env vars), `docs/operations/canary-runbook.md` (4-step canary 10→30→60→100 with explicit grpcurl + observability checks + abort/undo commands + Slack escalation), `docs/operations/go-live-checklist.md` (T-24h pre-flight + Infrastructure + Vault + Observability + Security + GitOps + Canary + Day 2 + Sign-off table). **(b) Long-tail standards (10 artifacts):** 4 more ontology TTLs (payin/netreport/risk/position v1.2.0 with cross-namespace `owl:imports`); 3 more RFLW flow files (`trade/RFLW.024.002.01.md` Settle FX Trade via CLS PvP + `eod/RFLW.024.050.01.md` End-of-Day Batch 4-step orchestration + `risk/RFLW.024.060.01.md` Pre-Trade Risk Limit Check with par-block for breach event); 3 more pattern catalogs (`201-fx-ddd-patterns.md` 4 docs incl. Aggregate Root + Reference by ID + Optimistic Concurrency + Domain events, `202-fx-eda-patterns.md` 4 docs incl. Transactional Outbox + At-least-once + consumer idempotency + In-process bus + Event naming, `206-fx-kafka-patterns.md` 4 docs incl. Idempotent producer config + Topic naming + partition_key + ACL per identity). Patterns README updated: catalogs now show 5+5+4+4+4+5 = 27 documented patterns across 6 catalogs. **(c) ISO 27001 audit prep (5 security docs):** `iso27001-controls-mapping.md` (all **93 Annex A controls** mapped to evidence files/release versions/owner — 67% implemented + 5% partial + 19% deferred to cert engagement + 5% inherited from GCP + 3% N/A; target ≥ 90% before audit), `threat-model-stride.md` (STRIDE × DREAD scoring for 4 components — exchangeos-api 6 threats + worker 3 + CRDB 2 + Vault 2 + Kafka 2 = **15 threats** all mitigated; 3 critical + 9 high + 4 medium/low), `sod-matrix.md` (Segregation of Duties — 7 roles × 23 actions matrix with ✓/✗/Δ-4-eyes annotations + 4 forbidden role-pair conflicts), `incident-response.md` (Sev1-4 classification + first-15-min + first-hour + post-incident process + 5 common scenarios S-1..S-5 + quarterly tabletop drills + contacts), `dr-runbook.md` (RTO 4h / RPO 5min targets + primary us-east1 + secondary us-central1 + 3 failover scenarios A/B/C + backup verification cadence + drill schedule + decision tree + comms template). **Dashboard unchanged at 100% (26/26) — these are post-plan operational + compliance artifacts, not new milestones.** |
| 4.15.0 | 2026-05-24 | **MAJOR — PLAN 100% DELIVERED (26 of 26 milestones)** — **(a)** Expanded MS-023m/n/o pattern catalogs to 5 docs each (added FX-GP-003/004/005 + FX-CP-002/008/009 + FX-DS-002/003/008 with Context/Problem/Solution/Example/Anti-pattern/Related per pattern) → all 3 milestones moved to delivered/. **(b)** MS-023h Production wrap-up: cert-manager ClusterIssuer (Let's Encrypt prod+staging + DNS-01 GCP CloudDNS for wildcards + HTTP-01 fallback + sample Certificate api.exchangeos.revenu.tech), SLSA L3 attestation workflow (`.github/workflows/slsa-attestation.yml` — buildx multi-arch + Cosign keyless OIDC + SBOM CycloneDX + `actions/attest-build-provenance@v2` push-to-registry + smoke `cosign verify` gating identity-regexp + OIDC issuer, per binary matrix × 7), ArgoCD Application + AppProject (`deploy/argocd/application.yaml` — automated sync prune+selfHeal + CreateNamespace + ServerSideApply + retry backoff 5×, AppProject RBAC scoped to revenu-platform sourceRepos + namespace whitelist) → moved to delivered/. **(c)** **All 10 remaining backlog milestones moved to delivered/** with detailed delivery notes mapping acceptance criteria to release versions where the work landed: MS-023i (Allenty Documentation — CLAUDE.md + version.md + CHANGELOG + workstream READMEs + module doc.go); MS-023p (API Contracts — 9 proto services + 8 gRPC adapters bound + REST smoke + topic catalog); MS-023q (IAM ISO 27000-27005 — cred-rotator + WIF zero-keys + TLS 1.3 + audit envelope + 93 Annex A controls cited); MS-023r (OTel native — pkg/telemetry.Init + OTLP/gRPC + zap structured + collector config); MS-023s (Local deploy + ~271 CRUD tests across 14 BCs); MS-023t (TDD + 10 E2E scenarios + 3 implementations + 30 security gates 3-tier + forbidigo); MS-023u (3 sync patterns: gRPC pull + in-process eventbus + transactional outbox kgo); MS-023v (Integration verification — Kafka catalog + CDC via outbox + gRPC service discovery + buf breaking + saga compensation + integration tests + canonical bounded-context template); MS-023w (Cross-platform — Task + Makefile + PowerShell + bash 3.2-safe + CI matrix [ubuntu/macos/windows] + multi-arch images); MS-023x (Pre-commit HARD enforcement — lefthook 3-tier + git-hooks-wrapper blocking --no-verify + EMERGENCY_BYPASS audit log + Slack alert + Conventional Commits gate). **Dashboard: 100% delivered (26/26). MAJOR version bump because this is the structural completion milestone.** Plan ready for closure + post-mortem ceremony. |
| 4.14.0 | 2026-05-24 | **MINOR — MS-023j/k/l + MS-023g DELIVERED (4 more, total 12) + MS-023m/n/o kickoff** — Massive delivery wave: **12 milestones delivered (was 8)**, **3 active (was 5)** — flipped from 31% to **46% complete** (12/26). **(a1)** MS-023j Ontology: 4 more representative TTLs (quote.ttl + refdata.ttl + cls_settlement.ttl + compliance.ttl, all OWL 2 DL v1.2.0 with FIBO `skos:closeMatch` + bilingual en/pt labels + `owl:imports` for cross-ontology references) → moved to delivered/ with notes. **(a2)** MS-023k Flows: 4 more RFLW.024.NNN.NN files following 10-section template (quote/RFLW.024.010.01 RFQ Streaming, cls_settlement/RFLW.024.020.01 CLS Daily Cycle 07:00→12:00 CET, compliance/RFLW.024.030.01 BACEN Classification+IOF on trade booked with par-block screening, cfets/RFLW.024.040.01 CFETS Trade Capture fxtr.031→032→033) → moved to delivered/. **(a3)** MS-023l ERDs: 4 more Mermaid erDiagram files (erd-quote-domain + erd-settlement-domain w/ cls_cycles+payin+net_reports + erd-risk-position-domain + erd-compliance-admin-domain w/ 6 tables) → moved to delivered/. **(b)** MS-023g full delivery: `tests/e2e/harness.go` with BaseURL/NewClient/WaitHealthy/Eventually/GET/GETQuery helpers (never time.Sleep — always Eventually with deadline) + 3 concrete E2E tests under `//go:build e2e` (scenario_01 EURUSD spot — refdata 18 CLS-eligible visible, scenario_05a/b risk breach — 404 + 400 error code propagation, scenario_08 BACEN — /version sanity) → MS-023g moved to delivered/ with notes attributing remaining 7 scenarios to REST surface expansion. **(c)** **3 pattern catalog milestones kickoffed** (MS-023m App layer / MS-023n Infra / MS-023o DevSecOps+IaC) with 1 representative catalog file per milestone: `200-fx-golang-patterns.md` (FX-GP 40 patterns, doc'd FX-GP-001 Aggregate constructor + FX-GP-002 Decimal precision with code examples + anti-patterns + related links), `205-fx-cockroachdb-patterns.md` (FX-CP 50 patterns with FX-CP-001 DECIMAL(36,18) + FX-CP-004 partial indexes), `210-fx-devsecops-cicd-patterns.md` (FX-DS 50 patterns with FX-DS-001 Lefthook 3-tier HARD + FX-DS-007 Argo Rollouts canary). `README.md` indexes all 20 catalogs totalling 850 planned patterns. **Cumulative delivered count: 12/26 = 46%.** |
| 4.13.0 | 2026-05-24 | **MINOR — MS-023d2/f/f2 DELIVERED + MS-023g full Kafka publisher (kgo) + MS-023j/k/l kickoff (Ontology/Flows/ERDs)** — **8 milestones now delivered** (was 5); **5 active** (was 5: MS-023g/h/j/k/l). (a) MS-023d2 (CFETS Capture + Confirmation) + MS-023f (Compliance Core + Admin) + MS-023f2 (BACEN Integration Suite) → `delivered/` with full delivery notes mapping acceptance criteria across releases 4.10.0..4.12.0 + correctly attributed deferred items (postgres compliance/admin repos, OFAC/UN/EU/COAF live providers, full 95-code BACEN catalog, DEC submission helper, SCE-IED/Credito/CBE adapters, SISCOAF COS XML). (b) **Real Kafka publisher** at `pkg/outbox/kafka/publisher.go` under `//go:build kafka` using `franz-go/kgo` v1.18.0 — production defaults: `acks=all` + zstd compression + idempotent producer + linger 5ms + 1 MiB batch max + 10k buffered records + 10s per-publish timeout. Compile-time interface check `var _ outbox.Publisher = (*Publisher)(nil)`. Added `github.com/twmb/franz-go v1.18.0` to go.mod. `cmd/worker/main.go` rewritten: now drives a real outbox dispatch loop against the postgres Store with backoff (500ms empty / 2s on error) + graceful shutdown via `signal.NotifyContext`. Worker idles if `repo_backend != postgres`. Publisher selection via build tags through paired files: `cmd/worker/publisher_default.go` (no tag — logs only, default-safe) and `cmd/worker/publisher_kafka.go` (`//go:build kafka` — reads `EXCHANGEOS_KAFKA_BROKERS` comma-list + `EXCHANGEOS_KAFKA_CLIENT_ID`, constructs `pkg/outbox/kafka.Publisher`, defers Close). (c) **3 standards milestones opened** (MS-023j Ontology, MS-023k Flows, MS-023l ERDs) — BACKLOG → ACTIVE. Created `.base/aasc/ontology/{core,bridges,shapes,compliance}/` structure + representative `core/trade.ttl` (OWL 2 DL profile, version 1.2.0, FIBO `skos:closeMatch` alignment, 5 FXTrade subclasses + 6 TradeStatus instances + 3 SettlementVenue instances + 4 ObjectProperties + 5 DatatypeProperties + bilingual en/pt labels + `dct:title`/`dct:description` + `owl:imports` LedgerOS finance ontology) + `README.md` documenting layout + versioning + validation (pyshacl + HermiT) + FIBO alignment target ≥ 80%. Created `.base/flows/{trade,quote,cls_settlement,cfets}/` + representative `trade/RFLW.024.001.01.md` (full Mermaid sequence diagram for Book FX Spot via CLS: Trader → API → PricingEngine → QuoteService → AcceptQuote → outbox → TradeService → RiskService.CheckLimit → BookTrade → SettlementService.AttachTrade with 9 numbered steps, error-path flowchart with 4 branch points (expired/limit/validate/save), Business Rules table citing RN_FX_001/002/010/026, Observability section, Compliance Notes, Related Patterns) + `README.md` documenting RFLW.024.NNN.NN naming + 10-section structure (YAML metadata + Description + Pre-conditions + Actors + Mermaid sequence + Mermaid flowchart + Business Rules + Observability + Compliance Notes + Related Patterns). Created `.base/erds/{domain,sql}/` + representative `domain/erd-trade-domain.md` (Mermaid erDiagram covering 6 entities — tenants/counterparties/fx_trades/trade_amendments/actors/audit_events — with full constraints + indexes + ontology cross-reference) + `README.md` documenting synchronisation rule with migrations/*.sql (lefthook pre-commit glob check TODO MS-023n). |
| 4.12.0 | 2026-05-24 | **MINOR — Wrap-up of 5 milestones + MS-023g/h kickoff** — **MS-023c, MS-023d, MS-023e moved to delivered/** (5 delivered total). MS-023g + MS-023h moved BACKLOG→ACTIVE. **(a1)** RiskServiceServer + PositionServiceServer gRPC adapters under grpcgen tag, registered in grpc_register_proto.go. **(a2)** Compliance application service (ClassifyOperation via pkg/bacen.Classifier ByCode-then-keyword-fallback, ComputeIOF via pkg/bacen.IOFCalculator, SubmitBACENReport, ScreenCounterparty) + memory repos (ClassificationRepo + IOFRepo + ReportRepo + ScreeningRepo) + ComplianceServiceServer gRPC adapter. Admin application service (EmitSystemEvent/ListSystemEvents/TriggerEOD with ErrConflict on duplicate (tenant, business_date) + Start/MarkStep/Complete/Fail EOD lifecycle) + memory repos (EventRepo + EODJobRepo) + AdminServiceServer gRPC adapter. **8 compliance application tests** + container wires `Compliance + Admin` in `wireComplianceAdmin()`. **(a3)** CFETSCaptureService + CFETSConfirmationService with shared mutate pipelines + memory repos + NoopPublishers; container wires both services. **(b1)** pkg/outbox/postgres.Store concrete impl against migration 000009 (Insert with nilToNew helper + nullable partition_key, Pending with ORDER BY occurred_at ASC LIMIT, MarkDispatched with archive copy best-effort, MarkFailed with 512-char truncation). `deploy/kafka/topics.yaml` — 14 Kafka topics catalogued (per BC events + refdata feeds with `compact` policy, defaults 32 partitions × 30d retention × zstd × min.isr=2 × RF=3) + ACL policy per service identity (5 SAs: api/worker/cls-cycle/eod/mq-bridge). `tests/e2e/README.md` — 10 canonical E2E scenarios catalogued (EUR/USD spot, USD/BRL NDF, CFETS capture/confirmation, risk breach, position update, CLS daily cycle, BACEN classification+IOF, sanctions screening, EOD batch). **(c1)** MS-023h Helm chart skeleton (`deploy/helm/exchangeos/`): Chart.yaml + values.yaml with per-binary config (api/worker/migrator/cls-cycle/eod/mq-bridge/cred-rotator with runAs: deployment/job/cronJob), templates for ServiceAccount/ConfigMap/Deployment-api/Service-api/HPA-api/PDB-api, distroless nonroot security context, External Secrets Operator integration for Vault, WIF annotations, Prometheus scrape, Argo Rollouts canary spec (`deploy/k8s/argo-rollouts/api-rollout.yaml`) with 4-step weight progression 10→30→60→100 + AnalysisTemplate Prometheus gates (5xx rate < 1% AND p99 < 500ms). Terraform GCP module skeletons (`deploy/terraform/modules/`): exchangeos-gke (Autopilot + KMS CMEK HSM for etcd + Binary Authorization + private cluster + master authorized networks), exchangeos-iam (WIF binding K8s SA → GCP SA + 6 least-privilege role assignments), exchangeos-network (VPC + subnet with secondary ranges for pods/services + Cloud Router + Cloud NAT). `deploy/terraform/environments/production/main.tf` composes the 3 modules with GCS backend. |
| 4.11.0 | 2026-05-24 | **MINOR — MS-023a/b DELIVERED + MS-023f/f2 kickoff (compliance + BACEN) + postgres repos for settlement/risk/position + pkg/outbox foundation** — Three milestone movements + 7 active total. **(a) MS-023a + MS-023b moved to delivered/** with full delivery notes (acceptance criteria items mapped to release versions; remaining gaps explicitly attributed to cross-repo CRDB hub PR for 023a and MS-023g Kafka outbox for 023b — neither blocks delivery of the in-scope work). **(b1) MS-023f kickoff** (BACKLOG → ACTIVE): `modules/compliance/domain` with 4 aggregates — Classification (BACEN 95-code nature, 4-6-digit numeric validation, REMESSA/INGRESSO/CONVERSAO), IOFComputation (notional×rate computed at construction, banker's round to 2 decimals, rate must be fraction ≤ 1), BACENReport (PENDING→SUBMITTED→ACCEPTED/REJECTED lifecycle, 3 ReportTypes SISBACEN/BCB-CCS/BCB-CAMBIO, payload_hash audit, version field), ScreeningResult (derived risk_level from hit count: 0→LOW, 1-2→MEDIUM, 3+→HIGH, IsClear/RequiresCOS flags for RN_FX_039 SISCOAF). **18 tests**. **(b2) `modules/admin/domain`**: SystemEvent (8 EventCodes mapping to admi.002/004/009/010/011/017 + internal CYCLE_OPEN/CYCLE_CLOSE/EOD_STARTED/EOD_COMPLETED, At defaults to now) + EODJob (PENDING→RUNNING→COMPLETED/FAILED with idempotent MarkStep for PTAX/MTM/POSITION_SNAPSHOT/BACEN_REPORT steps, 4 events). **6 tests**. Migration **000008_create_compliance_admin** (classifications + iof_computations + bacen_reports + screening_results + system_events + eod_jobs, all FK to tenants/fx_trades, CHECK constraints on enums + payload_hash, UNIQUE (tenant, business_date) on eod_jobs, partial index for HIGH-risk screenings). **(b3) MS-023f2 kickoff** (BACKLOG → ACTIVE): `pkg/bacen/classifier.go` with **20 most-common BACEN nature codes** (mercadorias/serviços/capital/transferências/turismo+cartão/outros) + free-text Classifier with keyword rules (export/import/royalty/investment/loan/travel/card/cross/derivative) + ErrUnknown. `pkg/bacen/iof.go` IOFCalculator with **6 canonical rates per Decreto 12.499/2025** (IOFExport 0%, IOFDefault 0.38%, IOFTravelCash 1.10%, IOFLoan 6.38%, IOFCreditCard 1.10%, IOFInsurance 6.25%) + 10 pre-seeded opTypes (EXPORT/DEFAULT/REMESSA/IMPORT/TRAVEL_CASH/TRAVEL_CARD/CREDIT_CARD/LOAN_SHORT/INSURANCE_FOREIGN/INVESTMENT) + Compute returning (rate, amount, err) with banker rounding to 2 decimals + extensible via `extra` rate maps. **15 bacen tests** (classifier ByCode + free-text matrix + bad inputs + IOF default/travel/export-zero/loan + bad-input matrix + extra-rates override). **(c1) Postgres CycleRepo** at `modules/cls_settlement/infrastructure/postgres/repos.go` with transactional Save (UPSERT cycle + DELETE+INSERT cls_cycle_trades), Get + FindByDate; `ReconstituteCycle` helper exposing all fields incl. 3 deadlines + closed_at + failure_reason. **(c2) Postgres LimitRepo + PositionRepo** with Reconstitute helpers; LimitRepo.Find resolves by (tenant, type, scope-uppercased); PositionRepo.Save uses ON CONFLICT (tenant, currency) DO UPDATE. Container wirePostgres calls new `wireSettlementPostgres(pool)` swapping memory→postgres for settlement+risk+position (payin+netreport still in-memory pending MS-023g). **(c3) pkg/outbox foundation**: migration **000009_create_outbox** (outbox_events + outbox_dispatched_archive, partial index `WHERE dispatched_at IS NULL`, composite index by aggregate, failed-row index for observability). `pkg/outbox/outbox.go` with `Record`/`Store`/`Publisher`/`PublisherFunc` + `Dispatch(ctx, store, pub, batchSize)` worker helper (default 100, ErrTopicMissing fast-path, per-record MarkFailed on publisher error, defaults partition_key to AggregateID). `pkg/outbox/README.md` documents pattern + integration recipe. **5 outbox tests** (happy 2-record batch, publisher error→MarkFailed, missing topic→ErrTopicMissing, empty batch no-error, default batchSize). **Total new tests: ~44**. |
| 4.10.0 | 2026-05-24 | **MINOR — MS-023d full stack (application + gRPC) + MS-023d2 kickoff (CFETS) + MS-023e kickoff (Risk + Position)** — Three new active milestones; **6 ACTIVE total** (MS-023a..e + MS-023d2). **(a) MS-023d application + infrastructure + gRPC**: `modules/cls_settlement/application/service.go` (OpenCycle with ErrConflict on duplicate (tenant, date), AttachTrade/EnterPayInWindow/EnterSettling/CloseCycle/FailCycle/GetCycle with shared mutate pipeline) + memory `CycleRepo` with `tenant:yyyy-mm-dd` uniqueness index + `NoopPublisher`. **5 cls_settlement tests**. `modules/payin/application/service.go` (Create/Submit/Confirm/Fail/Get/ListByCycle with publisher + memory repo sorting by currency+deadline). **5 payin tests**. `modules/netreport/application/service.go` (Generate/Get/ListByCycle, no publisher — read model). **3 netreport tests**. `modules/cls_settlement/api/grpc_server.go` (build tag grpcgen) full `pb.SettlementServiceServer`: OpenCycle, SubmitPayIn (Create+Submit chained), GetNetReport (placeholder XML), CloseCycle (EnterSettling+Close), `toPBCycle` with all 3 PIN deadlines populated via DeadlineFor + trade_ids slice, `toPBPayIn` with nullable submitted_at, mapErr translating ErrConflict→AlreadyExists, ErrDeadlineMissed→FailedPrecondition. Registered in grpc_register_proto.go. **(b) MS-023d2 CFETS** (BACKLOG → ACTIVE): `pkg/iso20022/fxtr/fxtr_cfets_031_038.go` with all 8 CFETS PTPP variant structs (CFETSTradeIdentification + CFETSEconomics shared blocks, fxtr.031 Capture Request, .032 Ack with `CFETSAckStatus` enum SUCC/REJT, .033 Notification, .034 Confirmation Request, .035 Confirmation, .036 Status with `CFETSConfStatus` PAIR/UPRD/REJT, .037 Amendment, .038 Cancellation) + 8 namespace constants `Namespace031..038`. doc.go status table now shows **15/15 fxtr ✅**. Bounded contexts `modules/cfets_capture/domain` (CFETSCapture aggregate: DRAFT → SUBMITTED → ACK/REJECTED → NOTIFIED, ack assigns CFETSDealID, 5 DomainEvents) + `modules/cfets_confirmation/domain` (CFETSConfirmation aggregate: CONFIRMING → CONFIRMED/UNPAIRED/REJECTED, MarkPaired allows from CONFIRMING or UNPAIRED, 4 DomainEvents). **8 capture + 7 confirmation tests** = 15. **(c) MS-023e Risk + Position** (BACKLOG → ACTIVE): `modules/risk/domain/limit.go` Limit aggregate (5 types COUNTERPARTY/CURRENCY/TENOR/DV01/VAR + Cap/Utilised/Available/UtilisationPct/Reserve with ErrBreached on overflow no-partial-reserve/Release clamped at zero/SetUtilised for reconciliation, scope required for type ∈ {Counterparty,Currency,Tenor}) + **9 tests**. `modules/risk/application/service.go` (CreateLimit/CheckLimit returning `CheckResult{Allowed,BreachedLimits,Explanation}`/Reserve/Release using Repository.Find(tenant,type,scope)) + memory repo with composite key + **5 application tests**. `modules/position/domain/position.go` (Position with Long/Short/Net/IsLong/IsShort/IsFlat + `TradeLeg{Side,Amount,At}` and ApplyTradeLeg defaulting AsOf) + **7 tests**. `modules/position/application/service.go` (Get/List/ApplyTradeLeg with upsert-on-miss creating fresh flat position) + memory repo + **4 application tests**. Migration **000007_create_risk_position** (`risk_limits` UNIQUE (tenant,type,scope) + CHECK enum + partial index for breached + check cap>0/utilised>=0; `positions` UNIQUE (tenant,currency) + as_of DESC index + check long/short >= 0). Container wires `Settlement/PayIn/NetReport/Risk/Position` in wireSettlement(); applies to both memory + postgres backends. **35 new tests** total this iteration. |
| 4.9.0 | 2026-05-24 | **MINOR — Real PricingEngine + MS-023c (Trade Core) + MS-023d kickoff (CLS Settlement + PayIn + NetReport) + event bus** — Three milestone movements + 4 active milestones total. **(a) Real PricingEngine**: `SpotRateBook` aggregate in `modules/refdata/domain` (thread-safe, TTL freshness, `Put`/`Lookup`/`LookupFresh` with `ErrNotFound`/`ErrStale`) + `modules/refdata/infrastructure/pricing.Engine` adapter (`SpreadPolicy` interface with `FlatSpreadPolicy` + `PerPairSpreadPolicy`) wired into container replacing stubPricing. Container now seeds 5 dev pairs (EURUSD/GBPUSD/USDJPY/USDBRL/USDCAD). **6 SpotRateBook tests** + **5 Engine tests**. **(b) MS-023c Trade Core opened** (BACKLOG → ACTIVE): full `modules/trade/application/service.go` (BookTrade/GetTrade/ListTrades/ConfirmTrade/CancelTrade/MarkSettling/MarkSettled use cases with shared `mutate` pipeline), in-memory + postgres TradeRepo (postgres resolves buyer/seller via `counterparties.bic` join), `Reconstitute` helper on FXTrade, gRPC adapter under `grpcgen` tag implementing all 5 TradeServiceServer RPCs with full enum-name mapping + error code translation. **7 trade application tests**. New HTTP smoke endpoint `/v1/trades/:id` in main.go. **(b3) Quote→Trade integration via in-process event bus**: `internal/eventbus/` (Bus + Subscribe/Publish + publisher adapters for quote + trade domains) + `modules/trade/application/from_quote.go` `QuoteAcceptedHandler` (reads quote.accepted.v1 → BookTrade with `AcceptedQuoteView` lookup) + container wires `EventBus`, switches Quote/Trade services from NoopPublisher → eventbus-backed publishers, registers handler in `wireEventHandlers()`. **2 container integration tests** including end-to-end `TestContainer_QuoteAccepted_BooksTrade` (GetQuote → AcceptQuote → assert trade exists in repo). **(c) MS-023d Settlement opened** (BACKLOG → ACTIVE): `modules/cls_settlement/domain/CLSCycle` (lifecycle OPEN → PAY_IN_WINDOW → SETTLING → CLOSED/FAILED, deadlines anchored to CET 07/08/09/10/12, idempotent AttachTrade with sorted IDs, optimistic version, 6 DomainEvent types), `modules/payin/domain/PayInInstruction` (PENDING → SUBMITTED → CONFIRMED/FAILED with auto-fail on missed deadline via `ErrDeadlineMissed`, 4 events), `modules/netreport/domain/NetReport` (gross_pay_in − gross_pay_out = net_settlement with IsReceivable/IsPayable helpers). **8 CLSCycle + 8 PayIn + 6 NetReport tests** = **22 new domain tests**. Migration **000006_create_settlement** (cls_cycles + cls_cycle_trades + payin_instructions + net_reports, all DECIMAL(36,18) for amounts, CHECK constraints on enum statuses + deadline ordering pin1<pin2<pin3<close, FK cascades, partial index for open cycles, composite indexes for status+deadline lookups). |
| 4.8.0 | 2026-05-24 | **MINOR — gRPC adapters (grpcgen tag) + OLINDA PTAXFetcher + Postgres repos + container backend switch** — Closes three MS-023b deliverables in one pass. **(a) gRPC binding** under `//go:build grpcgen` to avoid breaking default build: `modules/refdata/api/grpc_server.go` (ListCurrencies/GetCalendar/ResolveBIC/GetSSI with `mapErr` translating `ErrNotFound`→NotFound, `ErrInvalidInput`→InvalidArgument) + `modules/quote/api/grpc_server.go` (GetQuote with decimal-string Money parsing, AcceptQuote returning quote_id as trade_id placeholder until trade worker exists, StreamQuotes returning Unimplemented, `parseTenant` + `toPBQuote` helpers + error mapping incl. `ErrQuoteExpired`→FailedPrecondition). `cmd/api/grpc_register_default.go` (no-op) + `cmd/api/grpc_register_proto.go` (registers Refdata + Quote services under tag) pair; `buildGRPC` calls `registerGeneratedServices(srv, di)` unconditionally — workflow documented as `task proto:gen && go build -tags grpcgen`. Added `google.protobuf.Timestamp at_time = 4` to `GetSSIRequest` proto for completeness. **(b) OLINDA HTTP PTAXFetcher** in `modules/refdata/infrastructure/olinda/ptax_fetcher.go` hitting `https://olinda.bcb.gov.br/.../CotacaoDolarPeriodo` OData endpoint (MM-DD-YYYY URL-quoted dates), parsing `value[]` array, mapping by timestamp hour to the 4 windows (10/11/12/13). `parseOlindaTS` tolerates both `.000` and bare-second layouts. **6 tests** with `httptest.NewServer`: happy path producing 4 windows + WeightedFixing = 5.1028 ✓, missing-window-12 error, HTTP 500 propagation, zero-date guard, bad-timestamp parse error, URL format assertion (CotacaoDolarPeriodo + url-quoted 'MM-DD-YYYY' + `$format=json`). **(c) Postgres repos via pgx/v5**: added `Reconstitute*` helpers to all 6 aggregates (`modules/refdata/domain/reconstitute.go` for Currency/Calendar/BICRecord/SSI + `modules/quote/domain/reconstitute.go` for Quote/RFQ) bypassing constructor validation for safe DB-row hydration; documented as persistence-boundary-only. `internal/db/pool.go` pgx pool factory with 30min MaxConnLifetime + 5min MaxConnIdleTime + Ping-on-connect. `modules/refdata/infrastructure/postgres/repos.go` implements all 4 refdata repos against migrations 000004+000005 (parameterised queries, `COALESCE(...,'')` for nullable strings, calendar 2-query hydration of holiday set, SSI Find with `valid_from`/`valid_to` window + `ORDER BY valid_from DESC LIMIT 1`). `modules/quote/infrastructure/postgres/repos.go` implements QuoteRepo (UPSERT on Save) + RFQRepo (UPSERT + 2nd query to hydrate attached quote_ids from `quotes.rfq_id` FK). Added `Requester()/BaseCCY()/QuoteCCY()/CreatedAt()` accessors to `domain.RFQ` so postgres can read its state. **Container backend switch** via `EXCHANGEOS_REPO_BACKEND=memory\|postgres` (default memory; config validates postgres requires non-empty DSN). `internal/container/container.go` now exposes `New(ctx, cfg)` returning `(*Container, error)` + `Close()` for pool release; `wireMemory()` vs `wirePostgres(pool)` paths. `cmd/api/main.go` updated: container is context-aware + deferred Close + logs the active backend. Added `EXCHANGEOS_REPO_BACKEND=memory` to `.env.example`. Olinda tests: 6 new. |
| 4.7.0 | 2026-05-24 | **MINOR — PTAX + MTM + application layer + DI container + seeds + migration 000004** — Three-axis advance on MS-023b. **(a) Pricing closure:** `pkg/pricing/ptax.go` models BACEN 4-window survey (10/11/12/13h SP) with `WeightedFixing()` / `BidFixing()` / `AskFixing()` per Resolução BCB 277 (banker rounding to 4 decimals — BACEN display precision), `PTAXFetcher` interface + `PTAXFetcherFunc` adapter for testable HTTP-decoupling (OLINDA wiring deferred). `pkg/pricing/mtm.go` implements `Position{NotionalBase, BaseCCY, QuoteCCY, DealRate, MarketRate, Side LONG/SHORT}` + `PositionMTM` (LONG: `notional × (market − deal)`; SHORT: negated) + `PortfolioMTM` aggregating per-quote-CCY buckets. **18 new pricing tests** (PTAX: 4 + MTM: 7 + Portfolio: 1 + bad-input matrix). `pkg/pricing/doc.go` table: 6/7 algorithms ✅. **(b) Application layer wired in main.go:** `modules/refdata/application/service.go` with `CurrencyRepository`/`CalendarRepository`/`BICRepository`/`SSIRepository` interfaces + Service exposing ListCurrencies/GetCurrency/GetCalendar/ResolveBIC/GetSSI use cases (input normalisation + sentinel `ErrNotFound`/`ErrInvalidInput`). `modules/refdata/infrastructure/memory/repos.go` thread-safe bootstrap repos. `modules/quote/application/service.go` with `QuoteRepository`/`RFQRepository`/`PricingEngine`/`EventPublisher` interfaces + Service exposing GetQuote/AcceptQuote/CreateRFQ/AttachQuoteToRFQ/AcceptRFQ use cases (bid/ask derived from PricingEngine mid ± half-spread, TTL configurable, EventPublisher non-blocking errors swallowed per outbox semantics). `modules/quote/infrastructure/memory/repos.go` + `NoopPublisher` capturing published events. `internal/container/container.go` DI container with stubPricing placeholder. `cmd/api/main.go` invokes `container.New(cfg)` and exposes **`/v1/refdata/currencies?active_only=true`** smoke endpoint proving wiring end-to-end. **15 new application tests** (refdata: 7 covering List/ordering/active-filter + Get normalisation + bad-length + Calendar + BIC + SSI time-window picks latest active; quote: 8 covering GetQuote prices/persists/publishes + pricing-error propagation + bad-input matrix + AcceptQuote lifecycle/expired/not-found + full RFQ flow REQUESTED→QUOTED→ACCEPTED with 4 published events in order). **(c) Seeds 00-06 + migration 000004:** `migrations/000004_create_currency_pairs_netting.{up,down}.sql` (currency_pairs composite PK + spot_days 0/1/2 + CLS/CFETS eligibility + partial index; netting_cutoffs venue+ccy+band composite PK + CHECK band in PIN1/PIN2/PIN3/EOD). `seeds/00_tenants_dev.sql` (deterministic DEV tenant UUID), `01_currencies.sql` (30 currencies: 18 CLS + BRL + CNY/CNH + NDF universe + 3-decimal middle east), `02_currency_pairs.sql` (32 pairs: G10×USD + G10 crosses + China + EM-NDF + BRL crosses, USDCAD/USDMXN at T+1), `03_calendars.sql` (BACEN_BRL + NYFR_USD + BOE_GBP + TARGET2_EUR + TOKYO_JPY + TORONTO_CAD with 2026 holidays — illustrative for tests, production must source from authoritative feeds), `04_counterparties.sql` (36 BIC records: CLS members + Brazilian banks + CFETS reference), `05_ssi.sql` (5 sample SSIs for DEV tenant — no-op if DEV missing), `06_netting_cutoffs.sql` (24 CLS PayIn deadlines per CCY mapped to PIN1 Asia-Pacific 08:00 CET / PIN2 Europe 09:00 / PIN3 Americas 10:00 + bilateral EOD per CCY + CFETS CNY). `seeds/README.md` documents order, idempotence, source-of-truth caveats. Cumulative test count adds 33 to **125+ total**. |
| 4.6.0 | 2026-05-24 | **MINOR — pkg/pricing extensions: crossrate + NDF + tenor ladder** — Three new pricing algorithms in `pkg/pricing/` advancing MS-023b. `crossrate.go` with `Pair` type + `Pair.Invert()` (1/Rate with division-by-zero guard) + `Cross(a, b Pair)` triangulation that auto-detects the shared pivot currency and auto-inverts when alignment requires (handles all 4 corner cases). `ndf.go` implements USD-settled Non-Deliverable Forward per ISDA EMTA template: `SettlementAmount = notional_ref × (1/fixing − 1/contract)`, with sign convention documented (positive = dealer long ref-CCY receives USD when ref CCY appreciates; cited for BRL/CNY/INR/KRW/RUB/TWD/IDR/MYR — RN_FX_005). `tenor.go` defines 15 standard FX tenor codes (ON/TN/SN/Spot/1W/2W/3W/1M/2M/3M/6M/9M/18M/1Y/2Y) + `ParseTenor` (case-insensitive trim) + `ValueDate(tenor, tradeDate, BusinessCalendar)` honouring Modified-Following convention (forward to next BD; fall back to previous BD if forward roll crosses month boundary). `BusinessCalendar` local interface keeps `pkg/pricing/` clean of `modules/` dependencies — `refdata.Calendar` satisfies it implicitly. **24 unit tests added**: crossrate (7: EUR/USD/BRL happy path + auto-invert second + auto-invert first + GBPJPY large + identical pair rejection + no-shared-currency rejection + invalid pair + round-trip invert), NDF (5: BRL devaluation = −3921.57 USD, BRL appreciation = +4081.63 USD, ATM = 0, symmetry property within 10% magnitude ratio, 5 bad-input cases), tenor (7: ParseTenor 4 variants + invalid, ON/TN/Spot/SN basics, weekend-trade rolls forward, 5 standard tenors covering 1W/1M/3M/6M/1Y modified-following, ModFollowing fallback to previous BD when 3 consecutive holidays cross month). `pkg/pricing/doc.go` algorithm table now shows 4/7 ✅. Cumulative pricing test count: **35**. |
| 4.5.0 | 2026-05-24 | **MINOR — Closes iso20022 struct coverage for MS-023a + opens MS-023b (RefData + Pricing + Quote)** — Completes remaining 4 fxtr CLS messages (008 Trade Notification, 013 Position Statement, 017 Status Report with FXTradeStatusCode enum RCVD/PAIR/UPRD/STGN/SETT/RESC/REJT, 030 Settlement Notification with bought/sold settled amounts + payment instrument refs). Adds 3 new packages: `pkg/iso20022/admi/` (7 message types: 002/004/009/010/011/017/024), `pkg/iso20022/camt/` (4 message types: 061 PayInSchedule with PIN1/PIN2/PIN3 deadline bands, 062 Ack, 063 Cancel, 088 NetReport with per-CCY GrossPayIn/GrossPayOut/NetSettlement), `pkg/iso20022/reda/` (4 message types: 060/061 SSI request/confirm, 066 Calendar reference data, 067 SSI bulk update). **MS-023b OPENED:** moved `MS-023b-refdata-pricing-quote.md` BACKLOG → ACTIVE. Implemented `pkg/pricing/cip.go` — Covered Interest Parity forward formula per iotafinance (`F = S · (1 + i_p · n/N_p) / (1 + i_b · n/N_b)`) with 8-decimal internal precision + banker's rounding + ForwardPoints helper + **12 unit tests** (4 golden cases: EURUSD 90d, USDBRL 30d, GBPUSD 180d mixed 360/365 basis, USDJPY 365d; 4 property tests: days=0 returns spot, equal-rates-equal-basis no-points, positive carry, negative carry; 1 points-equals-fwd-minus-spot identity; 7 validation guards) + benchmark `BenchmarkForwardSimple` (target < 5µs). Implemented `modules/refdata/domain/` with 4 aggregates: Currency (ISO 4217 alpha-3 + minor_units 0/2/3 + CLS/CFETS eligibility + active flag), Calendar (holiday set per venue ID + IsBusinessDay + NextBusinessDay + AddBusinessDays walker), BICRecord (ISO 9362 structural validation across prefix/country/location/branch segments + LEI 20-char check), SSI (per-tenant standing instructions with valid_from/valid_to window + IsActiveAt + IBAN length 15-34) + **11 unit tests**. Implemented `modules/quote/domain/` with 2 aggregates: Quote (bid/ask with Mid helper, validity window IsActiveAt, NotionalCCY must match base or quote, Accept emits trade event) + RFQ (REQUESTED → QUOTED → ACCEPTED|REJECTED|EXPIRED lifecycle, AttachQuote/Accept/Reject/Expire transitions, optimistic concurrency version) + 8 DomainEvent types versioned + **10 unit tests**. Migrations `000003_create_quotes.{up,down}.sql` (rfqs + quotes + quote_streams, DECIMAL(36,18), full check constraints) and `000005_create_refdata.{up,down}.sql` (currencies + calendars + calendar_holidays + bic_records + ssis, refdata global except per-tenant ssis with composite-index optimised lookups). |
| 4.4.0 | 2026-05-24 | **MINOR — fxtr CLS structs (014/015/016) + XSD download script + round-trip test** — `pkg/iso20022/fxtr/` package with shared common types (`Amount` (Ccy attr + decimal value), `Rate`, `ISODate`, `ISODateTime`, `PartyIdentification`, `SideIdentification`, `TradedAmounts`, `AgreedRate`, `SettlementInfo`, `AuditTrail`) + 3 CLS message structs (`FXTradeCaptureConfirmationV05`, `FXTradeAmendmentConfirmationV05`, `FXTradeCancellationConfirmationV05`) + namespace constants. **5 common-type unit tests** (Amount precision round-trip 12 decimals, Rate, ISODate, ISODateTime, empty-amount-as-zero) + **2 envelope round-trip tests** (full marshal→Unmarshal via `marshaller` + registry covering 13 sanity assertions, unknown URN error). `scripts/download-xsd.sh` portable bash 3.2+ script: extracts 32 URLs via grep from `sources.go` (URLs refactored to explicit literals — no concatenation), validates HTTP 200 with retry, atomic write, sha256 manifest, OFFLINE mode for air-gapped/CI verification. `Taskfile.yml` adds `xsd:download` + `xsd:verify` targets; `Makefile` mirrors. Refactored `pkg/iso20022/registry/sources.go` from string-concat URL builders to explicit literals (32 lines, one URL per Descriptor) — same 32 schemas, same versions, but now grep-friendly for tooling. |
| 4.3.0 | 2026-05-24 | **MINOR — pkg/iso20022 toolkit + cmd/migrator wiring + first TDD aggregate** — Closes 2 MS-023a acceptance criteria gaps. (1) `pkg/iso20022/` toolkit: 32 FX-specific schemas registered (15 fxtr CLS+CFETS + 6 admi + 4 camt + 2 reda + 5 supporting) via `registry.Default()` with `Descriptor`/`URN`/`Key` model + `OrganisationRouter` (CLSBUS33/CFETS/ISO fallback) + 7 unit tests + `marshaller/` (head.001 BAH envelope) + `validator/` (XSD lightweight + `BusinessRule` runner aligned with SHACL shapes) + README. (2) `cmd/migrator` real golang-migrate/v4 wiring with cockroachdb driver, up/down/status/force/seed subcommands, no-op classification (`ErrNoChange`/`ErrNilVersion`), DSN normalization (`postgres://` → `cockroach://`), production DSN guard, dirty-state detection. (3) `modules/trade/domain/` first FXTrade aggregate (DDD root) covering RN_FX_001/002/010/026: NewTradeInput validation (positive amounts, distinct CCY ISO 4217, BIC length 8/11, value_date >= trade_date), full lifecycle PENDING→CONFIRMED→SETTLING→SETTLED + CANCELLED, optimistic concurrency `version` field, 5 DomainEvent types via `recordEvent` + `PendingEvents`/`MarkEventsCommitted` outbox hooks, **13 unit tests** in `_test` external package. Added `golang-migrate/migrate/v4 v4.18.1` to go.mod. |
| 4.2.0 | 2026-05-24 | **MINOR — Implementation kickoff (MS-023a moved BACKLOG → ACTIVE)** — Full repo scaffolding: 7 `cmd/` binaries, 14 BC `modules/` skeletons, 9 proto services compiling foundation (`buf.yaml`/`buf.gen.yaml`), CockroachDB migrations 000001-000002 (tenants/actors/audit + counterparties/fx_trades/amendments), `internal/{config,telemetry}` + `pkg/health` foundations (with unit tests), `cmd/api/main.go` dual HTTP :8094/gRPC :9094 with graceful shutdown, distroless multi-stage Dockerfile (target <50MB), local docker-compose (CRDB + Redpanda + OTel Collector + api), Taskfile primary runner + Makefile delegation + PowerShell mirror, lefthook 3-tier HARD enforcement + `scripts/git-hooks-wrapper.sh` blocking `--no-verify`, `.golangci.yml` strict + `forbidigo` `float64` ban, `.env.example`, CI workflows (`ci.yml` cross-OS matrix + `security.yml` gitleaks/govulncheck/trivy/CodeQL/SBOM) |
| 4.1.0 | 2026-05-24 | **MINOR — Delivery Dashboard adicionado** — `roadmap/delivery-dashboard.md` com snapshot executivo + sprint burndown + DORA metrics + ISO 27001 certification roadmap + cost savings tracking. Auto-update via `.claude/scripts/generate-delivery-dashboard.sh` + GitHub Actions hourly cron + Grafana dashboard JSON `docker/grafana/dashboards/exchangeos-delivery.json` + slash command `/dash` |
| 4.0.0 | 2026-05-24 | **Reorganizacao estrutural completa** — Monolitico v3.11.7 (10.499 linhas) quebrado em 12 workstreams (00-11) espelhando pattern LedgerOS. Snapshot v3.11.7 preservado em `_archive/allenty-v3.11.7-monolithic-plan.md`. Adicionado `version.md`, `CHANGELOG.md`, `README.md`, master `index.md`, per-workstream `index.md`, `roadmap/master-plan.md`, 24 milestones individuais `milestones/backlog/MS-023*.md` |
| 3.11.7 | 2026-05-24 | Pre-Commit HARD Enforcement (zero GitHub Actions desperdicado) — §22 + Fase 15P + 25 FX-COMMIT-* patterns |
| 3.11.6 | 2026-05-24 | Cross-Platform Tooling (Task + Makefile + PowerShell + Bash POSIX) — §21 + Fase 15O + 20 FX-XOS-* patterns |
| 3.11.5 | 2026-05-24 | Integration Audit (Kafka × DB × gRPC × Sync × 13 modulos) — §20 + Fase 15N + 25 FX-INT-* patterns |
| 3.11.4 | 2026-05-24 | Database Sync Pattern + Native AccountOS/PaymentOS — §19 + Fase 15M + 40 FX-SYNC-* patterns |
| 3.11.3 | 2026-05-24 | TDD + E2E + Security Local Gates pre-push — §18 + Fase 15L + 35 FX-QA-* patterns |
| 3.11.2 | 2026-05-24 | CRUD Tests Database Local + Deploy Local — §17 + Fase 15K + 40 FX-TEST-* patterns |
| 3.11.1 | 2026-05-24 | OpenTelemetry nativo Go — §16 + Fase 15J + 60 FX-OTEL-* patterns |
| 3.11.0 | 2026-05-24 | IAM nativo Identos + KeycloakOS + ISO 27000-27005 — §15 + Fase 15I + 50 FX-IAM-* patterns + 8 docs ISO |
| 3.10.1 | 2026-05-24 | API Contracts Suite (gRPC + REST + AsyncAPI) — Fase 15H + 150 patterns (55 FX-GRPC + 50 FX-API + 45 FX-ASYNC) |
| 3.10.0 | 2026-05-24 | DevSecOps + Supply Chain + IaC + K8s + Docker + Terraform + GCP — Fase 15G + 150 patterns + 8 GitHub Actions workflows |
| 3.9.9 | 2026-05-24 | Patterns CockroachDB + Kafka + Apache Flink — §14.7-9 + Fase 15F + 150 patterns infra/data |
| 3.9.8 | 2026-05-24 | Patterns Suite Go/DDD/EDA — §14 + Fase 15E + 120 patterns (40 FX-GP + 35 FX-DDD + 45 FX-EDA) |
| 3.9.7 | 2026-05-24 | ERDs Suite (`.base/erds/`) — §13 + Fase 15D + 23 ERDs + 16 SQL DDL CockroachDB |
| 3.9.6 | 2026-05-24 | Flows Suite (`.base/flows/`) — §12 + Fase 15C + 85 flows RFLW.024.* |
| 3.9.5 | 2026-05-24 | Ontology Suite (`.base/aasc/ontology/`) — §10/11 + Fase 15B + 35 TTL v1.2.0 |
| 3.9.4 | 2026-05-24 | BACEN Regulatory Coverage 100% — §2.6 + Fase 9B + 24 business rules + 8 docs |
| 3.9.3 | 2026-05-24 | Pricing & Algorithms Module — §2.5 + Fase 4P (iotafinance CIP + NDF + PTAX) |
| 3.9.2 | 2026-05-24 | ISO 20022 verificacao oficial — `fxti`/`fxmt` NAO existem; apenas `fxtr` |
| 3.9.1 | 2026-05-24 | Catalogo XSD por organisation submissora (CLS + CFETS) com pinning de versao |
| 3.9.0 | 2026-05-24 | **DRAFT inicial — Allenty v3.9.0 ExchangeOS** baseado em pattern Allenty v3.8.0 OnboardOS+AccountOS |

## Sources of Truth

- **Monolithic snapshot (v3.11.7):** [`_archive/allenty-v3.11.7-monolithic-plan.md`](../_archive/allenty-v3.11.7-monolithic-plan.md) — preserved as historical reference
- **Master index:** [`index.md`](./index.md) — entry point for navigating modular plan
- **Roadmap:** [`roadmap/master-plan.md`](./roadmap/master-plan.md) — high-level vision + milestones timeline
- **Milestones:** [`milestones/`](./milestones/) — 24+ milestones individuais
- **Per-workstream indexes:** 00-11 folders with `index.md` each

## Bump Triggers

- **PATCH (X.Y.Z+1):** typo, link fix, content clarification, version bump in a single document
- **MINOR (X.Y+1.0):** new document added, new milestone created, new pattern catalog
- **MAJOR (X+1.0.0):** structural reorganization, workstream hierarchy change, breaking convention change
